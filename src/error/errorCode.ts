export const ErrorCode: { [key: string]: { code: string, msg: string } } = {
    UserNoDefind: { code: 'UserNoDefind', msg: "The user does not exist." },
    AuthFailed: { code: 'AuthFailed', msg: "Authentication failed." }
}